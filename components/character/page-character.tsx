import React from 'react';
import { Text, View } from 'react-native';

type Props = {};

const PageCharacter: React.FC<Props> = ({}) => {
  return (
    <View>
      <Text>PageCharacter</Text>
    </View>
  );
};

export default PageCharacter;
